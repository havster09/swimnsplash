// Import Actions
import {  } from './ListingsActions';

// Initial State
const initialState = {};

const ListingsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default ListingsReducer;
